# Проверка photo_selection2

Дата: 2026-09-17. Окружение: `D:\PySM3_Codex\envs\pysm\python.exe`.

## Область изменений

Созданы `selection_import`, `selection_copy`, `selection_assignments` и `_common`.
Обновлён статус плана разработки. Базовый `photo_selection` и вызываемые им общие
модули не изменялись: SHA-256 проверен для 22 исходных файлов относительно снимка
перед переносом, включая пользовательские правки `app.py`.

Варианты `photo_selection2` и `photo_selection3` не использовались как источники.
Рабочий код версии 04 не импортирует приватные модули базового скрипта.
Только сравнительные тесты используют его действующее ядро как эталон.

`_common/assignment_core.py` побайтно совпадает с исходным
`FaceAnalysis/_common/photo_selection_core.py`. В `copy_service.py` сохранены
выбор кадров и структура подпапок, добавлены режимы `on_conflict` вместо ошибки
при несовпадении содержимого. Новый `copy_core.py` готовит выбранные файлы
без списка учеников, параметров сессии и расчёта назначений.

## Автоматические проверки

- После удаления вкладки импорта повторно пройдены все 48 тестов: таблица является
  непосредственным элементом разделителя, импорт/редактирование работают, старое
  поле `active_tab` не мешает восстановлению окна. У плана сохраняются две вкладки.
- До переноса: 21 тест базового скрипта и 1 тест его полного RAW-процесса — успешно.
- Новый комплект: 48 тестов — успешно. Проверяются импорт, CSV/AI, сохранение,
  конфликт параллельных импортных окон, эквивалентность копирования, RAW/XMP,
  настройки Capture One, `PH_`, исключённые папки, локации, конфликты файлов,
  частичная ошибка, повторное копирование, атомарность, публикация назначений,
  независимость действий и сохранение selection-файла.
- Проверены полные запуски всех трёх точек входа из другой рабочей папки:
  разрешение CLI; для №1/№3 — создание окна, цикл событий Qt и закрытие;
  для №2 — автоматическое копирование без Qt-приложения и Qt-потока.
- Проверены три `--help`, паспорта через загрузчик PySM и согласованность CLI.
- Созданный JSON прочитан существующим загрузчиком генератора альбомов.
- `compileall` для нового комплекта — успешно.
- Проверка пробелов выполнена адресно для новых текстовых файлов, с учётом CRLF.

Повторный запуск:

```powershell
$env:QT_QPA_PLATFORM='offscreen'
& D:\PySM3_Codex\envs\pysm\python.exe -m unittest discover -s scripts/Workflow2/Tools/photo_selection2/_tests -p '*_tests.py'
& D:\PySM3_Codex\envs\pysm\python.exe -m compileall -q scripts/Workflow2/Tools/photo_selection2
```

Тесты находятся в общей `_tests`, поскольку проверяют взаимодействие всех трёх
скриптов и их общее ядро. Имя `stages_tests.py` позволяет сохранять тест в Git
без изменения существующих правил игнорирования репозитория.

## Копирование без окна: проверка 2026-09-17

Полная точка входа №2 проверена в отдельном процессе с `PY_SCRIPT_MANAGER_ACTIVE=1`.
Проверяются начальный, промежуточный и конечный счётчики JSON-протокола прогресса,
его закрытие, код 0 при успехе/повторном запуске и код 1 при ошибке входов или записи.
Создание `QApplication` и запуск `QThread` в этой проверке явно запрещены.
Нет загрузки GUI назначения и image pipeline; файлы выбора/назначений не меняются.
Проверен отказ от копирования при изменении selection-файла во время проверки.

Это проверка протокола, принимаемого главным окном PySM; само главное окно PySM
для испытания не запускалось. После добавления `mode` все 43 теста прошли.

Полная точка входа копирования запускается с тремя обязательными путями и
`exclude_dirs=Masks` по умолчанию. Проверен запуск без файла списка учеников,
с некорректными matches/assignments JSON и неизвестным распознанным ID:
копирование от них не зависит. Проверяются объединение выбора нескольких
учеников, `PH_`, папка `unknown`, отказ при повреждённом выборе, небезопасной
локации и неоднозначном имени. В режиме `skip` существующий файл сохраняется
независимо от содержимого, соседний XMP копируется, итоговый код — 0.
Остальные два скрипта сохраняют прежние параметры.

Через полную точку входа проверены `overwrite` и `rename`, повторные запуски,
поиск первого свободного числового суффикса и сохранение JSON. Отдельно проверены
ошибка во время записи временного файла и отказ публикации при `overwrite`:
прежний файл не повреждается, временный файл удаляется. Проверены совпадение
источника и назначения во всех режимах и появление занятого имени между
проверкой и публикацией для `skip`/`rename`. Проверки проводятся на Windows.

## Проверка пользовательских изменений: 2026-09-17

Проверены переносимые импорты и новые отчёты. Исправлены неоднозначные `_common`
и `_lib`, закрывающие HTML-теги заголовков и возврат HTML из импортного окна.
Ошибка оформления отчёта копирования больше не меняет код успешной операции.
Новые заголовки и дерево ссылок сохранены.

Все 48 тестов прошли. Дополнительно проверены загрузка трёх точек входа при наличии
посторонних `_common`/`_lib`, полные запуски из перенесённой копии комплекта,
однократный отчёт импорта и несохранённые изменения, ошибка отчёта после успешного
перемещения и полный запуск копирования с запрещёнными импортами PySM/Qt.
GUI проверялся через Qt offscreen, без запуска главного окна PySM.

## Перемещение: проверка 2026-09-17

Через полную точку входа проверен `--mode move`: перенос RAW/XMP/настроек Capture One
и `PH_` с сохранением структуры, исключённые и невыбранные файлы, все три режима
конфликтов, повторный запуск, JSON и прогресс. Инъекцией ошибок проверены отказ
записи/публикации, отказ удаления исходника, продолжение обработки остальных файлов
и изменение исходника во время операции. При совпадении источника и назначения
файл пропускается во всех сочетаниях `mode` и `on_conflict`.
Междисковый перенос на реальных дисках не испытывался; реализация всегда использует
временную копию в назначении и последующее удаление.

## Проверка GUI первоначальной версии: 2026-09-16

В видимых окнах Windows с `QT_QPA_PLATFORM=windows` выполнены действия QTest:

- ввод номеров через настоящий модальный редактор импортного окна;
- первый RAW-проход, имитация экспорта, финальное копирование и создание плана;
- JPG-процесс, повторный импорт, обновление открытых окон, повторное копирование;
- переход между двумя вкладками, разворачивание дерева через действие меню;
- загрузка JPG-предпросмотра и экспорт HTML/CSV.

В первоначальной версии проверены отсутствие редактирования/очистки выбора в окнах №2/№3, отсутствие
копирования в №3 и отдельные ключи геометрии трёх окон. Снимки импортного окна,
копирования и обеих вкладок плана просмотрены визуально. Временные диалоги
подтверждения в автоматических сценариях получают заранее заданные ответы.
С 2026-09-17 окно №2 удалено; проверки RAW/JPG теперь вызывают его операцию без окна,
а GUI-проверки и сохранение геометрии относятся только к №1/№3.

## Границы проверки

Все фотографии и JSON в проверках синтетические, во временных папках. RAW-файлы
проверяют перенос байтов; экспорт Capture One имитируется тестовыми JPG.
Реальные фотосессии, Capture One и InDesign не запускались. Существующие workflow-наборы
не изменялись; новые экземпляры скриптов нужно включать в них отдельно.
