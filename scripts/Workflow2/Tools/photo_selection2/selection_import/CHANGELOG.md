# История изменений selection_import

## 2.2026.09.17

- Общая папка комплекта переименована из `Photo_Selection04` в `photo_selection2`; обновлены пути в тестах, руководствах и workflow-наборах.
- Удалён столбец «Ответ» и его флажки. Столбец «Источник» перенесён на освободившееся место; формат `photo_selection.json` сохранён.
- Удалена единственная вкладка «Импорт»: таблица размещена непосредственно в левой части разделителя. Сохранение геометрии поддерживает окно без вкладок и ранее сохранённые настройки.
- Исправлены пакетные импорты и запуск из другой рабочей папки; комплект переносится вместе с `_common` без зависимости от дерева `scripts/Workflow2/Tools`.
- Сохранены новый заголовок и отчёт со ссылками. Исправлен закрывающий HTML-тег; отчёт возвращается общему обработчику и выводится один раз. Восстановлено сообщение о несохранённом выборе.
- Уточнены общие параметры этапов: список учеников и параметры сессии нужны импорту и плану, но не копированию.
- В руководстве уточнено: скрипт копирования работает без отдельного окна; интерфейс этого скрипта не изменён.

## 2.2026.09.16

- Создан отдельный скрипт «Импорт выбора фотографий» на основе базового `photo_selection`.
- Импорт и ручное редактирование выбора учеников. Сохраняет только photo_selection.json.
- Общие компоненты размещены в `Photo_Selection04/_common`; интерфейс поддерживает темы PySM.
