# Скрипт "Редактор списков" (v2026.06.20)

## 1. Назначение

**"Редактор списков"** — приложение с графическим интерфейсом для формирования списка класса/группы для фотосъемки. Скрипт позволяет:
*   Импортировать неструктурированные текстовые списки.
*   Автоматически разделять имена и фамилии (Smart Parsing).
*   Управлять услугами и рассчитывать итоговую стоимость.
*   **Новое:** Добавлять любую дополнительную информацию (цитаты, соцсети, даты) через динамические поля.
*   Генерировать отчеты (HTML, CSV) и файлы для автоматизации (TXT).

---

## 2. История изменений

### Версия 2026.06.20 (Текущая)

- Каждая запись ученика получает постоянный `student_id` вида `A7K3-S001`.
- `*.list` хранит идентификатор списка и счётчик новых записей.
- Файл `{wf_photo_session}_children.txt` содержит только `student_id` в порядке съёмки.
- Общий fallback-файл `children.txt` больше не создаётся.
- AI-обогащение сопоставляет пользовательский текст с ФИО, но применяет ответ только по проверенному `student_id`.
- CSV содержит отдельную колонку `student_id`.
- Старые `*.list` без идентификаторов не поддерживаются и должны быть созданы заново.

### Версия 2026.01.11
-   **Архитектура:** Полный рефакторинг кода. Приложение разделено на модули для повышения стабильности.
-   **Доп. информация (Info Fields):**
    -   Реализована поддержка динамических полей (например, "Цитата", "Instagram").
    -   Добавлен "Редактор схемы полей" (добавление, удаление, переименование, сортировка столбцов).
    -   Добавлен параметр запуска `--wf_default_info_fields` для автоматического создания стандартных полей.
-   **Интерфейс:**
    -   Новый диалог редактирования информации о студенте с поддержкой **многострочного текста** и **навигации** (`Ctrl` + стрелки) по списку без закрытия окна.
    -   Улучшен дизайн диалоговых окон.
-   **Экспорт HTML:**
    -   Добавлен режим "Полный HTML" (включает вывод полей доп. информации).
    -   Внедрено версионирование шаблонов: скрипт автоматически обновляет файл `_list_template.html`, если он устарел.
-   **Логика:** Столбец "Стоимость" заменен на "Итого", который автоматически суммирует базовую цену услуги и стоимость всех дополнительных опций.

### Версия 2025.10.16
-   Добавлены редакторы конфигурационных файлов (`_services.json`, `_names_normalization.json`).
-   Внедрены горячие клавиши (`Ctrl+N`, `Delete`, `Ctrl+T`).

---

## 3. Параметры запуска

Скрипт принимает следующие параметры:

-   **`wf_dest_dir`**: Папка по умолчанию для сохранения файлов.
-   **`wf_output_txt_file`**: Полный путь к TXT-файлу идентификаторов учеников текущей фотосессии. Если путь не задан, TXT не создаётся.
-   **`wf_autosave_formats`**: Форматы автосохранения (`html`, `txt`, `csv`).
-   **`wf_default_info_fields`** (Новое): Список строк. Пример: `["Цитата", "Любимый предмет"]`. Если указано, эти колонки будут созданы в таблице автоматически при запуске.

---
	
## 4. Работа с данными

### 4.1. Идентификаторы учеников

При создании нового списка формируется короткий идентификатор списка, например
`A7K3`. Каждая новая запись получает следующий постоянный номер:

```text
A7K3-S001
A7K3-S002
A7K3-S003
```

`student_id` не зависит от фамилии, имени, алфавитного порядка и порядка съёмки.
Редактирование этих данных не изменяет ID. Номер удалённой записи повторно не
используется.

ID является техническим полем и не редактируется в основной таблице. Операция
«Сохранить как» сохраняет идентичность списка и все существующие ID.

### 4.2. Основная таблица
Таблица теперь поддерживает **динамические колонки**.
-   **Базовые колонки:** № съемки, Фамилия, Имя, Услуга, Доп. услуги, Итого.
-   **Пользовательские колонки:** Появляются справа, если вы добавите их через меню "Настройки".
-   **Расчет стоимости:** Колонка "Итого" не редактируется вручную. Она вычисляется автоматически: *Цена услуги + Сумма доп. услуг*.

### 4.3. Дополнительная информация (Цитаты и др.)
Для работы с данными, отличными от имени и цены, используется механизм Info Fields.

1.  **Настройка полей:**
    *   Перейдите в `Настройки -> Настроить поля информации...`.
    *   Здесь можно создать новые поля (например, "Цитата"), переименовать их или изменить порядок. Изменения применятся ко всей таблице.

2.  **Заполнение данных:**
    *   **Вариант А:** Писать прямо в ячейки таблицы (для коротких текстов).
    *   **Вариант Б:** Использовать расширенный редактор.
        *   Выберите ученика и нажмите `Ctrl+I` (или ПКМ -> Редактировать информацию).
        *   Откроется окно с большими текстовыми полями.
        *   **Навигация:** Используйте кнопки "Предыдущий/Следующий" или клавиши `Ctrl + ←` / `Ctrl + →` для переключения между учениками без закрытия окна.

---

## 5. Экспорт

### 5.1. HTML Отчет
Теперь есть два варианта сохранения (в меню "Файл"):
1.  **Сохранить HTML:** Создает компактный отчет (только имена и услуги).
2.  **Сохранить полный HTML:** Добавляет в отчет блок с дополнительной информацией (цитаты и т.д.) под именем каждого ученика.

### 5.2. CSV
При экспорте в CSV добавляется техническая колонка `student_id`. Все
пользовательские поля (Цитата, Хобби) добавляются как новые столбцы в конце
файла. Кодировка файла — UTF-16LE, разделитель — точка с запятой (`;`).

### 5.3. Файл текущей фотосессии

Путь задаётся параметром `wf_output_txt_file` и обычно формируется PySM как
`{wf_photo_session}_{wf_children_file_name}`. В файл попадают только ученики с
заданным `shoot_order`, по одному полному `student_id` на строку и в порядке
съёмки.

Универсальный `children.txt` больше не используется. Если точный путь файла
фотосессии не передан, скрипт показывает предупреждение и не создаёт TXT.

### 5.4. AI-обогащение

Пользователь по-прежнему может передавать сведения по фамилии, полному ФИО или
в свободном тексте. В запрос AI включается справочник `student_id + ФИО`.

AI возвращает:

- `matched` — однозначно сопоставленные записи с `student_id` и данными `info`;
- `unresolved` — неоднозначные фамилии и записи без уверенного совпадения.

Приложение применяет только `matched` с существующими уникальными ID.
`unresolved` показывается пользователю и автоматически не изменяет список.

---

## 6. Горячие клавиши

| Сочетание | Действие |
| :--- | :--- |
| **Ctrl + N** | Добавить новую строку |
| **Delete** | Удалить выделенные строки |
| **Ctrl + T** | Поменять местами Имя и Фамилию (и их цвета) |
| **Ctrl + E** | Редактировать доп. услуги (Extra Services) |
| **Ctrl + I** | Редактировать доп. информацию (Info Editor) |
| **Ctrl + S** | Сохранить список |
| **Ctrl + P** | Печать HTML |

---

## 7. Технические детали

*   **Шаблоны:** Скрипт использует файл `_list_template.html` для генерации отчетов. Если вы обновили скрипт, а шаблон старый, программа автоматически создаст резервную копию старого шаблона (`.old.html`) и запишет новый актуальный шаблон.
*   **Конфиги:** Файлы настроек (`_services.json`, `_names_normalization.json`) хранятся рядом со скриптом.
*   **Формат `*.list`:** В корне хранятся `list_id` и `next_student_number`, а в каждой записи `students` — полный `student_id`.
*   **Совместимость:** Формат без `student_id` не загружается. Такие списки необходимо создать заново.
