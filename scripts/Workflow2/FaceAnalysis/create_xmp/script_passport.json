{
  "description": "Скрипт создает или обновляет XMP-файлы (сайдкары) для изображений\n<div style=\"{theme.tooltip_instance_args_block}\">\nЯвляется связующим звеном между этапом AI-анализа и программами каталогизации. ФИО получает из `*.list` по `student_id`, сохраняет реальные имена в стандартных полях, технические ID в `Iptc4xmpCore:SubjectCode`, имя сессии в IPTC Headline и имя фотосессии в IPTC Category.\n</div>",
  "category": "Метаданные",
  "author": "Андрей Пугачев | t.me/pugachev_fotodeti03",
  "version": "v2.2026.06.22",
  "command_line_args_meta": {
    "analysis_dir": {
      "type": "str",
      "description": "Полный путь к папке, содержащей JSON-файл результатов AI-анализа изображений",
      "required": true,
      "default": "{wf_session_path}/{wf_session_name}/Output/Analysis_{wf_photo_session}"
    },
    "image_dir": {
      "type": "str",
      "description": "Полный путь к папке с изображениями, для которых создаются XMP-файлы",
      "required": true,
      "default": "{wf_session_path}/{wf_session_name}/Capture/{wf_photo_session}"
    },
    "student_list_file": {
      "type": "file_path",
      "description": "Файл *.list — единственный источник фамилии и имени ученика",
      "required": true,
      "default": "{wf_psd_path}/{wf_session_name}/{wf_session_name}.list"
    },
    "all_threads": {
      "type": "int",
      "description": "Количество потоков для параллельного создания XMP-файлов. Если 0 — используется количество ядер CPU.",
      "required": false,
      "default": "12",
      "min_val": 0,
      "max_val": 64
    },
    "landmark_enable": {
      "type": "bool",
      "description": "Включить данные 2D-ландмарок (106 точек) в XMP",
      "required": false,
      "default": false
    },
    "scan_folder_mode": {
      "type": "bool",
      "description": "Запуск скрипта в режиме рекурсивного сканирования папки с изображениями и сопоставление имён файлов из этой с данными в JSON",
      "required": false,
      "default": false
    },
    "xmp_subfolder": {
      "type": "bool",
      "description": "Сохранять XMP файлы в подпапке 'XMP' относительно целевого изображения",
      "required": false,
      "default": false
    }	
  }
}
