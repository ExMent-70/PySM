{
  "description": "Интерактивный поиск и копирование фотографий по номерам. Показывает всех учеников фотографии по `student_id`, получает ФИО из `*.list`, безопасно переименовывает одиночные портреты и при необходимости синхронизирует `matches_portrait_to_group.json` по ID.\n\n<b>RAW</b>: source_dir = <i>{wf_session_path}/{wf_session_name}/Capture/{wf_photo_session}</i>\n<b>JPG</b>: source_dir = <i>{wf_session_path}/{wf_session_name}/Output/Analysis_{wf_photo_session}/JPG</i>",
  "category": "Метаданные",
  "author": "Андрей Пугачев | t.me/pugachev_fotodeti03",
  "version": "v2.2026.06.21",
  "command_line_args_meta": {
    "analysis_dir": {
      "type": "dir_path",
      "description": "Путь к папке с результатами анализа (содержащей info_faces.json).",
      "required": true,
      "default": "{wf_session_path}/{wf_session_name}/Output/Analysis_{wf_photo_session}"
    },
    "source_dir": {
      "type": "dir_path",
      "description": "Путь к папке с исходными файлами для поиска.",
      "required": true,
      "default": "{wf_session_path}/{wf_session_name}/Capture/{wf_photo_session}"
    },
    "student_list_file": {
      "type": "file_path",
      "description": "Файл *.list — единственный источник фамилии и имени ученика",
      "required": true,
      "default": "{wf_psd_path}/{wf_session_name}/{wf_session_name}.list"
    },
    "dest_dir": {
      "type": "dir_path",
      "description": "Корневая папка для результатов (обязательна только для операции копирования).",
      "required": false,
      "default": "{wf_session_path}/{wf_session_name}/Selects/{wf_photo_session}"
    },
    "min_digits": {
      "type": "int",
      "description": "Минимальное количество цифр в номере для поиска.",
      "required": false,
      "default": 4
    },
    "max_digits": {
      "type": "int",
      "description": "Максимальное количество цифр в номере для поиска.",
      "required": false,
      "default": 4
    },
    "exclude_dirs": {
      "type": "string_multiline",
      "description": "Список подпапок для исключения из поиска (каждая с новой строки).",
      "required": false,
      "default": "Masks"
    },
    "rename-files": {
      "type": "bool",
      "description": "Включить операцию переименования файлов при копировании.",
      "required": false,
      "default": false
    },
    "copy-files": {
      "type": "bool",
      "description": "Включить операцию копирования файлов в папку назначения.",
      "required": false,
      "default": false
    }
  }
}
